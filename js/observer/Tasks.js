export const tasks = {
  walk(data) {
    console.log(`${data} Can Walk...`);
  },
  talk(data) {
    console.log(`${data} Can Talk...`);
  },
  fly(data) {
    console.log(`${data} Can Fly...`);
  },
};
