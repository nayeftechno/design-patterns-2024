export const dailyRoutine = {
  walk() {
    console.log(`${this.type} can walk...`);
  },
  talk() {
    console.log(`${this.type} can talk...`);
  },
  run() {
    console.log(`${this.type} can run...`);
  },
};
